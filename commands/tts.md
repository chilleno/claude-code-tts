---
description: Toggle text-to-speech for replies (on/off/status)
allowed-tools: Bash
---

TTS toggle result: !`bash "${CLAUDE_CONFIG_DIR:-$HOME/.claude}/hooks/tts-toggle.sh" $ARGUMENTS`

The toggle above has already been applied. Reply with exactly the toggle result line shown above and nothing else.
